import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'VIP-Connect-iOS-Ionic-Example',
  webDir: 'www'
};

export default config;
